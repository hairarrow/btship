import React, { FC } from "react";
import styled from "styled-components";
import analytics from "../../analytics";

const MastheadStyles = styled.footer``;

const Masthead: FC = () => {
  return <MastheadStyles>Hello</MastheadStyles>;
};

export default Masthead;
